# 翻譯 ledger

目前為空。M0/M1 只完成 ROM 身分、執行期輸入與 OBJ 文字畫面消費證據，尚未解出可穩定抽取的日文 source table，因此沒有建立 placeholder 或未審核譯文。

可提交的 ledger 只會保存 `source_locale`、`source_hash`、stable ID、`zh-Hans`／`zh-TW` 譯文與審核 metadata，不保存完整 `source` 原文。建立第一批前，先完成 `research/ledger-plan.md` 所列的 decoder、術語查證、控制碼與寬度驗收。
