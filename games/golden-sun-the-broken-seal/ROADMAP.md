# 工作路线

## 1. 建立可重复构建

- [x] 保存日版原 ROM、上游 BPS 和公开测试版构建。
- [x] 记录基准校验值。
- [x] 保存旧汉化版参考样本与本地备份。
- [ ] 增加一键构建、打包、校验脚本。
- [ ] 增加构建产物清单与自动校验脚本。

## 2. 逆向文本系统

- [x] 比较日版与 2024 新汉化版的 ROM 布局。
- [x] 定位日版 Huffman 文本、新版直接字符串表及字体渲染钩子。
- [ ] 比较旧汉化版的 ROM 布局与文本系统。
- [ ] 完成自定义中文码页与字库映射。
- [ ] 编写无损文本提取器和回插器。
- [ ] 对未修改文本执行提取后回插，验证 ROM 行为一致。

## 3. 文本修订

- [ ] 建立人物、地点、精神力、精灵、道具和怪物术语表。
- [ ] 建立问题单：场景、原文、现译、建议、截图和状态。
- [ ] 先处理错字、标点、溢出和明显误译。
- [ ] 再处理语气、语域与全局术语统一。
- [ ] 若制作繁体版，先审计字库覆盖率和界面宽度，避免机械简繁转换。

## 4. 程序与兼容性

- [ ] 收集可稳定复现的异常，不根据模糊描述猜测补丁地址。
- [ ] 在 mGBA 与至少一个独立的模拟器或 GBA 相容环境中分别验证。
- [ ] 区分游戏缺陷、汉化缺陷、模拟器缺陷和存档版本问题。

## 5. 发布

- [ ] 以校验正确的日版原 ROM 为基准生成新的 BPS。
- [ ] 发布补丁、变更记录、校验值和已知问题，不发布商业 ROM。
- [ ] 保留上一稳定构建，实验版通过验证后才标记为可发布版本。

## 6. 從日版直接漢化（獨立路線，比照第二部）

- [x] 確認日版原 ROM 版本與第二部一致的定版來源（`AGSJ01 rev 0` 對應 `AGFJ01 rev 0`），逐位元組核對雜湊。
- [x] 用共用的 `core/golden-sun/extract-huffman-text-ids.rb` 抽出全部 11,115 條日文字串，與既有審計結果一致。
- [x] 確認基礎假名表可與第二部共用；發現擴展字形表不可跨作沿用（見 `research/jp-codepage-derivation.md`），改以字形點陣比對推導出第一部自有碼表，114 個擴展字形全數比對成功並通過語意 sentinel 檢查。
- [x] 交付第一批試譯（`translations/system-messages.draft.jsonl`，id 0–30 存檔 UI，31 條，通過 schema 驗證）。
- [x] 擴充至 id 0–1350（原 1,045 條，後因角色預設名 bug 移除 7 條降為 1,038 條，另 306 條佔位／除錯標籤依慣例保留），涵蓋戰鬥指令、商店／設定、武器防具道具資料庫、精靈與召喚名稱；建立 `tools/build_translation_batch.py` 做「原文比對重用」批次生成，可重複執行擴充後續區間。
- [x] 逆向第一部原始字型渲染／Huffman／文字指標的程式參照位址（每個角色兩處字面量，見 `README.md`「建置成果」），建立 `tools/expand_rom.rb`＋`tools/build_zh_tw_trial.rb`。
- [x] 定位第一部 ROM 內可用的空白擴展區（`0x800000`，ROM 補零擴充至 10,485,760 bytes）。
- [x] 建置可燒錄 BPS、執行 `verify_text_delta.rb` 與 BPS 往返校驗，全數通過；mGBA 實機 QA 確認字型／指標渲染管線正常，並發現並修復角色預設名姓名緩衝區單位元組截斷 bug（見 `README.md`）。
- [x] 強化 `tools/build_translation_batch.py`：重用第二部譯文前同時核對控制碼結構（換行位置、`{HH}` 插值、終止碼），不只比對顯示文字是否逐字相同；並排除全 ROM 範圍內 744 條原文為單一半形「?」、無控制碼的除錯佔位字串。全 ROM 範圍掃描後新增 `translations/shared-corpus-reuse.draft.jsonl`（1,764 條）。
- [x] 擴充翻譯批次覆蓋全部真人劇情對話（id 1498–11011 區間，21 個批次、7,317 條，由平行子代理逐批新譯，每批獨立驗證後 commit）。累計 10,119／11,115（91.0%）；剩餘 996 條為 ROM 內部場景／房間除錯標籤與除錯佔位符，依既有慣例不翻譯。
- [x] 執行完整建置＋`verify_text_delta.rb`＋BPS 產生與往返驗證，全數通過（見 README「建置成果」最終驗證表）。
- [x] 以直接讀取 ROM 內字型點陣資料渲染多組跨批次字串（含最早／最晚 id 範圍）驗證畫面正常性；未進一步走到實際遊玩路徑觸發戰鬥／商店／存檔選單畫面。
- [ ] 交叉檢查：第二部語料庫的角色預設名（id 131–137 一類）是否有同樣的姓名緩衝區截斷 bug（見第二部 ROADMAP 第 7 節）。
- [ ] 剩餘 996 條除錯標籤／佔位符逐一覆核，確認皆為引擎內部使用、玩家不會看到的內容（目前僅以「非日文顯示字元」啟發式排除，未逐條人工確認）。
- [ ] 全體 10,119 條譯文的人工／真機複核（目前皆為 AI 草稿狀態 `ai_draft`，多批次翻譯過程中已發現並修正約 20 處跨批次專有名詞不一致，但翻譯品質本身尚未經人工逐條審閱）。
