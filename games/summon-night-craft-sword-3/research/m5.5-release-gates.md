# M5.5 後發布 gate 收斂：runtime transport 與 static render chain

本文件是 M5.5 consistency 收尾後的發布 gate 記錄，不是新的翻譯批次。
M5.5 已經是最後一個同長度、重複通用短句的 static slice；目前維持既有八筆
`zh-TW`／`ai_review` target，不新增 M5.6 或其他同型 target。

## 既有八筆 review

`research/m5-static-review.md` 逐筆檢查八個 stable `string_id` 的語意、target
hash、單行／byte-length、控制碼、glyph allocation 與 adjacent static contract：

- 八筆可維持目前的 provisional wording；`b3cj:t2:025:0x0b6e` 的語氣詞仍保留
  idiomatic alternative note，尚不升格為 `approved`。
- `b3cj:t2:024:0x0078` 的 `重金礦` 已由兩個臺灣社群資料中的 `重金鉱` 來源名
  交叉支持；簡體資料的 `重金矿／重金矿石` 不作 zh-TW 主流依據。glossary
  改為 `provisional`；ledger 現為 `ai_review`，不冒充外部人工定稿。
- 八筆已完成本 session 的語意、術語、字型與版面 review，但仍不是外部人工核准，
  也不代表畫面 review 或發布資格；不得用重複短句增加數量。

## Compile-time fixed-port transport receipt

在不重用 `-C gdb.port` 的前提下，使用既有 `/private/tmp` SDL build 做一次不同
的 compile-time route。source checkout 的固定 revision 是
`afd6f14eaf8bd35214ed3fb9dc69a92bfc3877a9`；該 checkout 的 dirty diff 只把
`src/debugger/debugger.c` 的 `GDBStubListen` port 從 `2345` 改為 `2346`，並有
既存的 macOS deployment-target build diff。實測 binary 為：

- binary：`/private/tmp/mgba-source.B4BE9x/build-fe6-gdb-final/sdl/mgba`
- binary SHA-256：
  `08c8b810bf1d0b279c8e3839ab56950ae5c61fe57928d122de3eeea66048d9bf`
- version：`0.11-1-afd6f14eaf8bd35214ed3fb9dc69a92bfc3877a9-dirty`
- ROM：本作 ignored clean B3CJ；`B3CJ`／32 MiB／CRC32 `12afae5d`／clean SHA-
  256 `39bc4cf448106aa4b8cdde235632ffb57432c4b1919c8843510b70b3787fad2d`
- port `2346`：啟動前 `lsof -nP -iTCP:2346 -sTCP:LISTEN` 無 listener。
- launcher：
  `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy <binary> -g <clean B3CJ ROM>`
- result：foreground process 輸出 `Debugger: Couldn't open socket`，事後
  `2346` 仍無 listener；本 session 的 foreground execution handle 為 `38665`，
  由本 session 發送 Ctrl-C 正常停止，沒有停止或占用其他 session 的 process。

因此這輪仍在 socket bind 前失敗，沒有 `qSupported`、GDB connection、breakpoint、
watchpoint、consumer hit 或 live memory。這是 transport-only negative，不是 ROM、
glyph 或譯文失敗；不再以同一個 bind 方法重試。先前 `gdb.port`／Qt 及本輪 SDL
compile-time route 的差異與錯誤邊界均保留在 `research/recon-ledger.md`。

## Static writer → VRAM／palette hardware-copy receipt

`tools/audit_static_render_destination.py` 以 clean B3CJ identity、十三個本機 function
range hash 與十六個 literal pool guard fail closed。重跑命令：

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  games/summon-night-craft-sword-3/tools/audit_static_render_destination.py \
  games/summon-night-craft-sword-3/roms/base/B3CJ-jp-from-zip.gba \
  --output games/summon-night-craft-sword-3/work/m2.9-static-render-audit.json
```

ignored report SHA-256 是
`e38a2e8a5bc7d7ee10c41321132d01b42bef03c8f3d817be6afbe0fbfa5598ec`。

本地 ROM 與固定 csm3 commit `7e388ac861bbac289b1f86dc5b8fa46d47b1a1a2`
交叉成立的 static chain 是：

1. `sub_080036F8` 經 `sub_0800348C` lookup 呼叫 `sub_08002CB4`；writer 的
   `r1` 是 glyph output buffer，per-glyph stride／output span 為 `0x80`。
2. `sub_0800B730` 將該 output buffer、text-window field
   `gUnk_030056C0 + 0x194` 與 `glyph_count << 7` 交給 `sub_08006BA4`。
3. `sub_08006BA4` 將 source／destination／length 寫入
   `gUnk_03002DC0` descriptor queue；`DmaCopyMapAndPltt` 將 descriptor 寫入
   GBA DMA register `0x040000D4`。
4. `sub_0800D81C` 對本作 text window index 0 將該 destination field 初始化為
   `0x06010000`，再進入 `sub_0800B730`。因此本輪可確認的是 writer → queued DMA
   → VRAM character/tile-data region 的 static destination chain，不是 live hit。
5. `sub_080036C4` 從 ROM palette source table `0x08B6D610` 取值，經
   `sub_0800F224` 寫入 `gUnk_03005960` palette shadow formula
   `base + variant*0x0c + palette_index*0x20`，使用 indices `0x0e`／`0x0f`。
6. csm3 的 `sub_08001C00` main-loop callsite（本機 function hash guard）呼叫
   `sub_08010CD4`；該函式從 `gUnk_03005960 + 0x400 = 0x03005D60`，以
   `0x400` bytes 排入同一 `sub_08006BA4` DMA queue，目的為 `0x05000000`。
   因此 hardware palette destination 已有 confirmed-static evidence，但仍沒有
   live palette readback 或畫面色彩證據。
7. 本機 `sub_08001BC0` 呼叫 local `sub_080092CC` OAM transfer equivalent；
   function／literal guards 證實 `0x030038B0` → `0x07000000`、`0x400` bytes、
   DMA control `0x84000100`。這是 OAM buffer copy 的 static evidence，不是文字
   glyph/OAM 使用或畫面結果。
8. 本機 `sub_08009654` 與固定 csm3 `copy.c` formula 證實 text tile-data address
   `(((var->unk2 & 0x3ff) + var3->unk2) * 0x20) + 0x06010000`，stride `0x20`
   bytes（4bpp）、mask `0x3ff`、table root `0x030040C0`；不把它升格為 tilemap
   placement。

尚未由這條 bounded chain 證實 tilemap destination、live OAM values/layout、live font
cache、自然 reachability、VRAM bytes、screen hash 或 glyph readability。OAM transfer
與 text tile address 只作 static proof；tilemap／runtime 仍 unknown/false，hardware
palette 仍只作 destination proof，沒有把 static chain 升格為 runtime QA 或可發布 patch。

## Static text-object to OAM supplement

為了把「OAM copy 存在」與「文字 renderer 會建立 OAM metadata」分開，
`tools/audit_static_text_oam.py` 先重跑上述 audit，再以本機 B3CJ 的兩個額外
function hash 與五個 literal guard 驗證：

1. `sub_0800D81C → sub_0800B730` 是既有 text-window entry；`sub_0800B730`
   在 `sub_080036F8 → sub_08002CB4` 之後，按 glyph count 建立每字元 `0x28`-byte
   object descriptor。
2. main loop 的 `sub_08001C00 → sub_0800901C` 取用
   `gUnk_03003CC0`／`gUnk_030037B0`，將每個 object 的三個 OAM attribute
   halfword pack 到 `gOamBuffer=0x030038B0`。
3. 既有 `sub_08001BC0 → sub_080092CC` guard 再把該 buffer 接到
   `0x07000000`／`0x400`-byte hardware OAM DMA。

這個 supplement 讓 text-object→OAM buffer 的 static control-flow 有獨立收據，
但不把它升格為 live OAM、畫面或 glyph 可讀性證據；tilemap destination 仍
unknown。完整 hash／重跑命令見 [`research/m5-static-text-oam.md`](m5-static-text-oam.md)。

## Semantic multi-resource container rebuild

`tools/rebuild_container.py` 已對固定 13 個含 record resource、11 個 unique payload
groups 做 semantic no-op rebuild：每個 PSI3 token（含 opaque span）先 byte-identical
重發，再以 deterministic GBA LZ77 encoder 壓回原 pointer span，尾端 zero-fill。完整
收據見 [`research/m5-container-rebuild.md`](m5-container-rebuild.md)。

- 361/361 records 的 raw／record identity 與 `32092` stream bytes 保持一致；source
  Shift-JIS re-encode `361/361`，opaque `203`、rejected marker `1`。
- type-2 directory byte-identical；8191 changed bytes 全在既有 payload spans；rebuilt
  ROM SHA-256 `badb5838efafc3eb585c271208281bf44ce120ab46ec1ad573b4ddd55d185793`、CRC32
  `5344e805`，header checksum 未變。
- BPS `8615` bytes、SHA-256
  `326217ae194287d394fda8ef0296e095dff378d0c034238a48cae9c848080034`，套用後與
  rebuilt ROM byte-identical。

這只關閉 semantic LZ77／PSI3 container no-op 層；既有翻譯仍只對 resource 24 做
變長／relocation static rebuild，不能把本收據稱為全遊戲翻譯回插或發布 patch。

## Full type-2 container coverage

`tools/rebuild_full_container.py` 再對固定 type-2 directory 的 79 個 entry 做完整
static coverage：68 個 non-zero、互不重疊的 positive payload groups 都通過 PSI3
parser/encoder no-op、deterministic LZ77 recompress 與原 span capacity guard；11 個
zero-span entry 被保留為三個已證實的 alias clusters，不獨立寫入。全 table 的
79-entry stream round-trip 與 361/361 logical source record re-encode 通過，directory
byte-identical，24507 bytes 差異只在 positive payload spans；26147-byte BPS apply
byte-identical。完整 receipt 見 [`research/m5-full-container.md`](m5-full-container.md)。

這把 semantic no-op coverage 擴到完整 type-2 table，但沒有把它升格為 translated
release encoder：變長 target、未知 VM／layout、general pointer relocation、runtime
screen 與 header／release policy 仍 blocked。

## Clean 2347 runtime receipt

在固定 csm3／B3CJ target 與 ignored M5.5 BPS artifact 上，另以乾淨 mGBA clone
build（source `26b7884bc25a5933960f3cdcd98bac1ae14d42e2`、compile-time port
`2347`、binary SHA-256 `b4488dc0ffad295ce97b579a4b1da543ddd7e4aac8a457f64aa00c58b49fbc17`）
完成一次 fresh process／single connection。`qSupported` 與 `S02` readiness 成功；
自然 boot/title 執行中觀察到 `sub_08010CD4` 與 `sub_08006BA4`，live register
contract 是 `0x03005d60 → 0x05000000`、`0x400` bytes。這只把 palette shadow 到
hardware destination 的一段升格為 live callsite 證據；`sub_0800D084`、
`sub_080036F8`、`sub_08002CB4`、`sub_0800B730` 在這個 bounded route 未命中。

第二個 fresh process 的一次 controlled `r1` argument-hijack 嘗試在 client
`connect()`、尚未送出 `qSupported` 前收到 macOS `Errno 49`，沒有 scratch write
或受控 hit。完整 PID／port／hash／coverage 表見
[`research/m5.5-runtime-qa.md`](m5.5-runtime-qa.md)；這是 transport/startup
negative，不是 ROM／譯文失敗。

## Clean 2348 memory-access receipt

為了不重複 dirty fixed-port 或 `gdb.port` 路徑，本 session 又以固定 mGBA
revision `26b7884bc25a5933960f3cdcd98bac1ae14d42e2` 建立獨立 clean clone，
只把 compile-time GDB port 改成 `2348`。本作專用 SDL binary
`/private/tmp/atlantis-b3cj-mgba-2348-build/sdl/mgba` 的 SHA-256 是
`fe11732d1686b66bb5304cdbe13636e7e9daf648196e13c2b5f110e71841cc9`。

- fresh own PID `26158` 先由 `lsof` 確認 `127.0.0.1:2348` listener；單一
  GDB connection 完成 `qSupported`（含 `qXfer:memory-map:read+`）與 `S02`。
- controlled writer probe 僅嘗試寫入自己 emulator 的 IWRAM scratch
  `0x03007000`，回應 `E06`；未設定 call argument，沒有 consumer／writer
  hit，也沒有讀取或保存 raw memory。
- fresh own PID `28984` 再次有 listener，但後續 probe 在第一個 packet 前
  收到 `Errno 49`；直接 AF_INET probe 亦同。把 mGBA 與 single client 放入
  同一 launcher 的替代路徑也回 `Errno 49` 且報告無 listener，已由本 session
  中止並清理。

這個結果把 runtime 證據精確分成「一次 clean bind／qSupported 成功」與「GDB
memory write／跨 process transport 不可用」兩層；沒有把 readiness 升格為
文字 renderer QA。changed `0x847/0x848/0x849`、adjacent `0x846` 的 live
cache／VRAM、palette readback、tilemap／OAM 與畫面可讀性仍 unknown。完整
transport chronology 見 [`research/m5-runtime-text-probe.md`](m5-runtime-text-probe.md)。

## Clean 2350 controlled writer → VRAM receipt

在上述歷史 2348 negative 之後，本 session 以同一固定 mGBA HEAD 建立 compile-time
port `2350` 的 clean SDL binary，先 preflight `127.0.0.1:2350`，再由本 session
啟動 fresh own PID `91895`。binary SHA-256 是
`1ad2f1087941ea95ccc33d96b6ed45cff30da64f34c51f58b1f3e9dd7f4a6fe1`；source
revision 是 `26b7884bc25a5933960f3cdcd98bac1ae14d42e2`。工具與完整 receipt 見
[`tools/runtime_m5_writer_probe.py`](../tools/runtime_m5_writer_probe.py) 與
[`research/m5.5-runtime-writer-qa.md`](m5.5-runtime-writer-qa.md)。

probe 使用 `core/gba/gdbstub_client.py` 的單一 connection、timeout `8s`、packet
delay `0.12s`、retry delay `0.35s`。M5.5 base／target／BPS／apply hash guard 先
通過；qSupported exact 為：

```text
swbreak+;hwbreak+;qXfer:features:read+;qXfer:memory-map:read+;QStartNoAckMode+
```

自然 boot 先觀察到 `sub_08006BA4` palette queue：`0x03005d60 → 0x05000000`、
`0x400` bytes，font pointer `0x03002984 → 0x094d5c88`。在該 stopped state 只做
一次明確 controlled call：以 memory-order `ec64 ec65 ec66 0000` 進入
`sub_080036F8`，在 `sub_08002CB4` 觀察 `0x847`／`0x848`／`0x849` 對應的 ROM
glyph pointers 與 0x80-byte outputs；再以 `sub_08006BA4` 排入
`0x0203f000 → 0x06010000`／`0x180`，由 `0x08006ac4` flush。live VRAM
`0x06010000..0x180` 非零 131 bytes，SHA-256
`bad69ba786472e0f0e12fa7e1ec98e54ec010239f8fc335019a7ae6743f211c0`，三段 VRAM
slice 與 writer output hash 一致。這是 controlled writer→queue→flush→VRAM 的
連續 live 證據，不是自然 consumer hit。

受控 physical adjacent `0x846` cell output 全零；palette readback `0x05000000`
為全零，`DISPCNT=0x1040`、`BG0CNT..BG3CNT=0x0000`。core renderer 的 32×24
ignored PPM hash 為 `71ef5ebe4c1fed34e36d5a27796f4f81d0a67ac04c82a3370d82f5c56f6c9c7d`，
畫面全黑，因此 palette／tilemap／screen readability 不通過。自然
`sub_0800D084`、target record reachability、live OAM values/layout 與自然相鄰
equality 仍 unknown；八筆 ledger 維持 `zh-TW`／`ai_review`，不新增 M5.6。

## Clean 2351 controlled handler callsite receipt

上一節的 2350 probe 直接控制 writer input；本輪改用固定 HEAD 的新 clean SDL
binary，在獨立 port `2351` 對既有 `sub_0800D81C` 做 bounded CPU-register-only
entry。source revision 為 `26b7884bc25a5933960f3cdcd98bac1ae14d42e2`，binary
SHA-256 為
`07d2439a560c510ee7b76f6ee72ee5a18faf899d4d791054e6bcd35ff5577b93`，fresh own
PID `19669`；啟動前後均由 `lsof` 確認 `127.0.0.1:2351`，未接管其他 session。

`tools/runtime_m5_handler_probe.py` 先通過 clean／target／BPS／apply hash guard，
再用單一 core GDB connection 完成 qSupported／`S02`。自然 palette queue 仍是
`0x03005d60 → 0x05000000`／`0x400`，font base `0x094d5c88`。受控設定只寫
`lr=0x0800d085`、`pc=0x0800d81c`，不寫 script 或 glyph input；因 mGBA PC-write
後的 breakpoint 行為，第一個 stop 是下游 `sub_0800B730`，不是 entry breakpoint。
bounded stop sequence 為：

```text
sub_0800B730 → sub_080036F8 → sub_08002CB4 × 6
```

這是第一次在本作 clean runtime 中，從既有 handler entry 的 controlled jump 收到
下游 text-window／writer／glyph-writer hits；它與 static callsite 互相支持，但
boot context 的 current-pointer 為零，故不是 target record、自然 consumer 或畫面
QA。return sentinel 未到達；受控 call 後由本 session 停止自己的 paused process，
mGBA 輸出 invalid-address／crash，只記為非目標 context 的 bounded abort。完整
hash-only report SHA-256 是
`d301d0e43a44ee21e7b125f760bbf8828e65caa1884581a09c1f713f0e25a2a1`，留在
`/private/tmp/b3cj-m5.5-handler-2351-probe.json`；tool/test 留在本作目錄。

這輪新增的證據層級是「transport confirmed／controlled downstream callsite
confirmed／natural target and screen unknown」，不升格 2350 已有的 controlled
writer→VRAM slice，也沒有提供 target／adjacent live VRAM equality、palette
usability、tilemap、live OAM 或可讀畫面。下一個最小 gate 仍是自然導覽命中 target
record，或完成足以替代畫面 QA 的 static tilemap／palette／OAM layout 與完整
translated release encoder；不新增 M5.6 或同型短句。

## Shared ROM identity／runtime-session adoption receipt

依 `docs/gba-localization-common-tools.md`，本輪不改寫既有 game-specific probe，
先把下一個 runtime case 接到共用工具：
`research/m5.5-runtime-session-case.json` 固定 M5.5 target SHA-256、size 與
`B3CJ` header code；manifest 不含 ROM path、source text 或 raw payload。

共用 identity command（target ROM／report 均 ignored）為：

```sh
PYTHONDONTWRITEBYTECODE=1 python3 scripts/gba-rom-identity.py \
  /Users/bmy001/Work/project-atlantis/games/summon-night-craft-sword-3/work/m5.5-repeated-prize-header-applied.gba \
  --expect-size 33554432 --expect-game-code B3CJ --expect-crc32 fc874c4d \
  --expect-sha256 acfb3587a8217bf4ea444daf25f32c0947998a9203ee874db5006d7b6b016db6 \
  --output /private/tmp/b3cj-m5.5-target-identity.json
```

exit `0`、report `status=pass`；identity report SHA-256
`ab23138b0b0c2993a0029eb26fa4456dbc932bdb29000c54f109f045799360fb`。strict manifest
validation 亦 exit `0`。共用 session 先做 port `2351` preflight（exit `0`，status
`pass`），再以絕對路徑的 clean binary
`/private/tmp/atlantis-b3cj-mgba-2351-build/sdl/mgba` 與 target ROM 執行：

```sh
PYTHONDONTWRITEBYTECODE=1 python3 scripts/gba-runtime-session.py preflight \
  --host 127.0.0.1 --port 2351 \
  --session-report /private/tmp/b3cj-m5.5-session-preflight-2351.json

PYTHONDONTWRITEBYTECODE=1 python3 scripts/gba-runtime-session.py run \
  /Users/bmy001/Work/project-atlantis/games/summon-night-craft-sword-3/research/m5.5-runtime-session-case.json \
  --rom /Users/bmy001/Work/project-atlantis/games/summon-night-craft-sword-3/work/m5.5-repeated-prize-header-applied.gba \
  --mgba /private/tmp/atlantis-b3cj-mgba-2351-build/sdl/mgba --port 2351 \
  --runtime-report /private/tmp/b3cj-m5.5-session-runtime-2351.json \
  --session-report /private/tmp/b3cj-m5.5-session-2351.json \
  --log /private/tmp/b3cj-m5.5-session-2351.log \
  --env=SDL_VIDEODRIVER=dummy --env=SDL_AUDIODRIVER=dummy
```

run exit `0`；session report status `pass`、sole listener ownership `ready=true`、
PID `35308` 由共用 identity guard terminated；session report SHA-256
`33ebf6350f0b9857bac3a0703a70f125f3760a4ac985af583d5c5227823a4e5`。generic runtime
report status `pass`，required capabilities `gdb-remote`、register/memory read、
breakpoint 全部 exercised；report SHA-256
`750da486a766f6bf8948fd929ec626d998873bef9d5f67d3d7cc467d82f647f1`。Breakpoint
register contract 為 `r0=0x03005D60`、`r1=0x05000000`、`r2=0x400`；capture hash-only
regions 為 font pointer `47c523da125de666267ecddf6961092e447dcb0de83ba93ec62a95e55eece5df`、
palette shadow `1a2c4eaa86fc86c48ee672ec01828c347d5ca35ed1d0a662d4c451a6e2667520`、
script-state pointer `8f1e12659ff92d53f321349db271c5274625693cbb915efdf2fe267949f499e6`。

這個 common-tool `pass` 僅表示 ROM identity、child command、sole listener、GDB
connection 與自然 palette queue/capture 成立；不表示 game text consumer、target
record、changed glyph VRAM／tilemap／OAM、畫面可讀性或發布 gate。`runtime_m5_handler_probe.py`
仍保留本作專屬受控 handler／writer／codepage 邏輯，未由共用 manifest 猜測或取代。

## 目前 gate 與下一個最小缺口

| gate | 狀態 | 證據邊界 |
| --- | --- | --- |
| 八筆 target static consistency | confirmed static / `ai_review` | session review、glyph／layout／BPS／re-extract 已重跑；八筆語意可維持 provisional，`重金礦` 有社群來源支持但仍待外部人工核准 |
| writer → DMA → `0x06010000` | confirmed static + controlled live | static function/literal chain，加上 clean 2350 `sub_080036F8`／`sub_08002CB4`→`sub_08006BA4`→`0x08006ac4`，三段 changed VRAM slice hash 對上；不是自然 consumer |
| palette | partial live / not usable | clean 2350 自然 queue `0x03005d60→0x05000000`／`0x400` 命中，但受控 readback 全零、BG0–3 全零；沒有 palette usability／畫面證據 |
| semantic multi-resource LZ77／PSI3 rebuild | confirmed static | 13 resources／11 payload groups 的 translated-scope no-op，加上全 type-2 table 79 entries／68 non-zero payload groups 的 no-op；361 records、directory unchanged、capacity guards、BPS apply byte-identical；不等於 translated multi-resource release encoder |
| text tile-data address | confirmed static | `sub_08009654` local hash/literals + csm3 formula；不等於 tilemap placement |
| OAM transfer path | confirmed static text-object chain | `sub_0800D81C` → `sub_0800B730` per-glyph descriptor → `sub_0800901C` → `gOamBuffer=0x030038B0`，再由 `sub_08001BC0` → `sub_080092CC` 複製至 `0x07000000`／`0x400` bytes；live values/layout 未讀 |
| tilemap | unknown | 本輪沒有 tilemap destination callsite，維持 fail-closed |
| mGBA/GDB runtime | partial live / controlled text path | clean 2350 fresh PID `91895` 的受控 target lookup／writer／queue／flush／VRAM hash，加上 clean 2351 PID `19669` 的 `sub_0800D81C` controlled downstream `sub_0800B730 → sub_080036F8 → sub_08002CB4`；自然 `0x0308` consumer／target record、tilemap、live OAM、screen readability 仍未命中 |
| 可發布 patch | blocked | 仍缺外部人工定稿、runtime 或等價完整畫面證據、全遊戲 coverage 與 release policy |

下一個有意義的工作只能關閉上述 gate（人工 review、允許 localhost 的 runtime、
更完整的 static tilemap/OAM callsite、或完整 container/release encoder）；
不再新增相同標頭或通用短句。

## Repository commit boundary

先前 `REPO-001` 所記錄的 index-lock permission negative 已由 path-limited commit
`87ed08a14a07e900c7593456b1c42f2ea400f792` 解決；該 commit 的 name-only scope
只有 `games/summon-night-craft-sword-3/**`，作者為 `JohnnySun <bmy001@gmail.com>`。
本次 runtime receipt 仍會在 commit 前重新核對 staged scope，不使用 alternate index、
commit-tree、reset、checkout 或 stash。
