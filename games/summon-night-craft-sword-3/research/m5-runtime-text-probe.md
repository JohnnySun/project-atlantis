# B3CJ text runtime probe receipt

This receipt records the next bounded runtime attempts after the clean 2347
palette callsite run. It adds no translation target and does not claim a live
text render or a publishable patch.

## Fixed runtime inputs

- target ROM: ignored M5.5 cumulative build
- base SHA-256: `39bc4cf448106aa4b8cdde235632ffb57432c4b1919c8843510b70b3787fad2d`
- target SHA-256: `acfb3587a8217bf4ea444daf25f32c0947998a9203ee874db5006d7b6b016db6`
- BPS SHA-256: `f0b030756e4744c5f5a42c8bb874341062c73df818ba86342e13a1cbe3c92b34`
- clean mGBA source: `26b7884bc25a5933960f3cdcd98bac1ae14d42e2`
- compile-time GDB port: `2347`
- binary SHA-256: `b4488dc0ffad295ce97b579a4b1da543ddd7e4aac8a457f64aa00c58b49fbc17`

The process was always a fresh process owned by this session and was stopped
after each attempt. Port 2345 was not touched.

## Attempts and coverage

1. A background launcher returned PID `99779` but exited before producing a
   listener or log output. No GDB packet was sent; this is a launcher lifetime
   negative.
2. A foreground fresh process reached the GDB greeting. The first probe
   progressed through readiness, breakpoints, and controlled-input phases but
   aborted in the probe's final memory summarizer when a non-hex `OK` response
   reached the old core-client read helper. No runtime claim was made from that
   incomplete report.
3. A fresh process with client host `127.0.0.1` failed at `connect()` with
   `OSError: [Errno 49] Can't assign requested address`; `qSupported` was not
   sent. This reproduces the prior controlled-connection negative.
4. A fresh process using the known successful host spelling `localhost`
   completed one GDB connection:

   - `qSupported`:
     `swbreak+;hwbreak+;qXfer:features:read+;qXfer:memory-map:read+;QStartNoAckMode+`
   - initial stop: `S02`
   - breakpoints set at local B3CJ `sub_0800D084`, `sub_080036F8`,
     `sub_08002CB4`, and `sub_0800B730`
   - ten bounded `S02` interrupt snapshots; no requested text breakpoint hit
   - controlled active-low KEYINPUT writes all returned `OK`: release `0x03ff`,
     A `0x03fe`, START `0x03f7`, and A+START `0x03f6`
   - KEYINPUT read itself returned a bounded two-byte response
   - GDB memory reads for `0x06000000` VRAM, `0x05000000` palette,
     `0x07000000` OAM, and `0x03000000` IWRAM all returned `E06`

The input route is explicitly **controlled** and is not natural reachability.
The ten stops were generated by the probe's interrupt timing, not by a text
consumer or writer breakpoint. No scratch argument hijack was used, and no
raw RAM/VRAM/OAM/palette dump or image was retained.

## Gate result

Transport is confirmed for the `localhost` route, but text consumer/writer
coverage remains unknown. Changed glyph slots `0x847/0x848/0x849`, adjacent
`0x846`, live font cache, live VRAM, tilemap/OAM state, and screen readability
remain unverified. The static v3 writer/tile-data/OAM evidence is not promoted
to runtime evidence. The smallest next runtime step is a separately scripted
natural navigation or controlled call that stops at the local text consumer;
until then the eight ledgers remain `ai_draft`.
